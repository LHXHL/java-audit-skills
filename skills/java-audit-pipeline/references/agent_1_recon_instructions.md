# Agent-1-recon：入口侦查与 worker 拆分

本文件只给 `java-audit-pipeline` 的 `agent-1-recon` 使用。它负责识别物理模块、入口配置文件和 route worker 拆分边界，不负责提取完整路由，也不估算漏洞风险。

## 职责

- 识别 Java 项目的物理模块、部署产物、编译产物、依赖目录和入口配置文件。
- 统计可精确复核的文件数量，例如页面文件、入口配置文件、已存在反编译源码、依赖文件。
- 生成 route worker 任务拆分，交给 `java-route-mapper` 继续提取完整路由。
- 标注需要下游 route worker 精确解析的文件，不提前写完整路由结论。

## 确定性计数

每个模块的数量必须使用确定性命令或等价文件枚举结果，不能凭经验手算：

```text
页面文件数：在模块目录下枚举项目实际页面后缀。
已存在反编译源码数：在模块目录下枚举可读反编译源码文件。
依赖文件数：统计该模块依赖目录下一层依赖归档数量。
入口配置文件数：统计该模块中被项目使用的入口配置文件数量。
```

写入报告前必须做总数对账。任一等式不成立时，不得输出“完成”；必须把冲突项写为“不可确认”并说明需要重新枚举。

## 输出文件

写入 `{output_path}/route_mapper/_recon/`：

- `project_overview.md`
- `module_inventory.md`
- `route_worker_tasks.md`

不得只写一个合并的 `recon_report.md` 代替以上 3 个文件。

## project_overview.md 模板

```markdown
# 项目侦查概览

## 1. 输入来源

| 项目 | 值 |
|---|---|
| source_path | {实际路径} |
| 统计时间 | {timestamp} |

## 2. 精确统计

| 指标 | 数量 | 计算依据 |
|---|---:|---|
| 物理模块数 | {number} | {文件枚举依据} |
| 入口配置文件数 | {number} | {实际路径列表} |
| 页面文件数 | {number} | {文件枚举依据} |
| 已存在反编译源码数 | {number} | {文件枚举依据} |
| 依赖文件数 | {number} | {文件枚举依据} |

## 3. 模块概览

| 模块 | 物理路径 | 类型 | 入口配置 | 页面数 | 反编译源码数 | 依赖数 |
|---|---|---|---|---:|---:|---:|
| {module} | {path} | {Java 模块/静态目录/空目录/不可确认} | {有/无} | {number} | {number} | {number} |

## 4. 限制说明

- {无法精确统计的内容；没有则写“无”}
```

## module_inventory.md 模板

```markdown
# 模块入口清单

## {module}

| 类型 | 数量 | 证据路径 | 说明 |
|---|---:|---|---|
| 页面文件 | {number} | {路径或文件清单位置} | {说明} |
| 入口配置 | {number 或 未精确统计} | {路径列表} | 交给 route worker 精确解析 |
| 编译产物 | {number 或 未精确统计} | {路径列表} | 仅作为反编译候选 |
| 依赖文件 | {number 或 未精确统计} | {路径列表} | 仅作为组件或运行时证据 |

### 下游注意事项

- {只写文件范围、机制线索和解析注意事项；不写预估路由数}
```

## route_worker_tasks.md 模板

```markdown
# Route Worker 任务拆分

## 1. 拆分依据

| 项目 | 说明 |
|---|---|
| 拆分粒度 | {按物理模块/按模块+入口机制} |
| worker 数 | {number} |
| 未精确统计项 | {无/列表} |

## 2. 任务清单

### agent-1-{N}: {module}

| 字段 | 值 |
|---|---|
| 模块 | {module} |
| 输入文件 | {入口配置/反编译源码/编译产物候选路径列表} |
| 输出目录 | 写本轮实际模块输出目录，使用绝对路径或相对 `output_path` 的真实路径 |
| 下游 skill | `java-route-mapper` |
| 必须提取 | {ENTRY_ROOT/DISPATCH_RULE/ENTRY_OPERATION/REQUEST_PARAM} |
| 不确定项 | {未精确统计项或 无} |

#### 交给 route worker 的要求

- 精确提取实际 route、operation、参数和证据路径。
- 不沿用 recon 阶段的机制线索作为路由结论。
- 无法精确枚举时写 blocked，不得写估算。
```

## 强制要求

- recon 阶段不写“预估路由数”。
- 不写估算词、波浪线、尾随加号、范围数量或模糊数量词。
- 输出文件不得保留 `{...}`、`${...}` 或其他模板占位符；所有路径必须替换为本轮真实路径。
- 数量必须来自文件枚举、配置节点计数、表格行数或其他可复核证据。
- 未统计或无法确认的数量写“未精确统计”，并说明缺失证据。
- 不生成漏洞结论、鉴权结论、组件风险结论、payload 或请求。
- 不把编译产物文件名猜测成路由；只能写“交给 route worker 精确解析”。
