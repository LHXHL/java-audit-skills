# 元数据策略鉴权

本文件不列具体注解、框架或库 API。Agent 应从项目证据中识别元数据如何映射到 `POLICY_RULE`。

## 检查点

- 元数据是否真正被 `AUTH_GATE` 读取。
- 元数据作用范围是类、方法、operation、资源还是字段。
- 缺省策略是允许、拒绝还是继承。
- 自定义组合元数据是否需要递归解析。
- 运行时是否存在覆盖、忽略或条件分支。

只有声明没有执行器时，不得写已鉴权。
