# 路由调用链追踪报告模板（完整版）

适用场景：命中敏感 sink、分支复杂、用户点名、pipeline 下游会读取，或需要完整证据的入口方法。

硬约束：

1. 输出必须包含 8 个主章节：`0` 到 `7`。
2. 章节顺序不得调整。
3. 所有 `【填写】` 必须替换为真实内容；无内容写 `无` 或 `未提供`。
4. 不得写 CVE、CVSS、漏洞已确认、攻击成功、修复版本等专项结论。
5. 测试请求只能使用安全样例值，不能生成破坏性 payload。
6. 文件建议放在 `route_tracer/{route_slug}/`。
7. 请求模板中禁止使用 `${param}`；结构占位只允许 `{{param}}`，安全样例请求必须填入实际低风险值。
8. 不得凭 Manager/DAO/Repository 方法名推断 SQL/FILE/XML 等 sink；缺实现时写 `UNCONFIRMED`。
9. 报告文件必须包含文件尾部的 `## 输出自检`；终端最终响应不能替代文件内自检。
10. 文件中禁止使用三个连续英文点号作为省略；代码片段必须摘录真实语句，不得用省略号占位。

---

# 【填写：项目名】 路由调用链追踪报告

## 0. 上游上下文

| 项目 | 内容 |
|------|------|
| 路由/入口 | 【填写：路由、operation 或用户指定方法】 |
| route_id | 【填写：若无填无】 |
| 上游来源 | 【填写：route_mapper / trace_batch_plan / 用户指定 / 其他】 |
| 鉴权状态 | 【填写：原样透传上游鉴权信息；未提供则写未提供】 |
| 鉴权风险来源 | 【填写：auth_audit/cross_analysis 文件或无】 |
| 本报告边界 | 只追踪调用链、sink 和可控性，不判定漏洞成立 |

## 1. 追踪对象

| 项目 | 内容 |
|------|------|
| 源码路径 | 【填写】 |
| 输出文件 | 【填写】 |
| 入口框架 | 【填写：Spring MVC/Struts2/Servlet/JAX-RS/WebService/用户指定】 |
| HTTP 方法 | 【填写：GET/POST/...；非 HTTP operation 写 SOAP/内部入口】 |
| 入口类方法 | 【填写：Class.method(signature)】 |
| 入口证据 | 【填写：注解、配置、route_mapper 文件、WSDL 等位置】 |
| 分析限制 | 【填写：无或具体限制】 |

## 2. HTTP/SOAP 请求与参数

### 2.1 请求模板

```http
【填写：结构化请求模板；可用 {{param_name}} 表示结构占位；禁止使用 ${param_name}；不使用攻击 payload】
```

### 2.2 安全样例请求

```http
【填写：可发送的安全样例值；用于说明参数结构，不代表漏洞 PoC】
```

### 2.3 参数定义

| 参数路径 | Java 类型 | HTTP/SOAP 位置 | 来源证据 | 说明 |
|----------|-----------|----------------|----------|------|
| 【填写】 | 【填写】 | 【填写】 | 【填写】 | 【填写】 |

## 3. 调用链证据

按真实调用顺序逐层填写，不得跳层。

| 层级 | 类方法 | 文件位置 | 关键调用/代码片段 | 参数传递 |
|------|--------|----------|------------------|----------|
| L1 | 【填写】 | 【填写】 | `【填写】` | 【填写】 |
| L2 | 【填写】 | 【填写】 | `【填写】` | 【填写】 |

### 3.1 调用链图

```text
【填写：入口 -> ... -> sink 的 ASCII 图，标注变量名变化】
```

## 4. Sink 识别

| Sink ID | Sink 类型 | Subtype | 位置 | 到达参数 | 关键代码 | 下游建议 |
|---------|-----------|---------|------|----------|----------|----------|
| S1 | 【填写：SQL/FILE_READ/FILE_WRITE/XML/DESERIALIZE/COMMAND/HTTP/LDAP/EXPRESSION/RESPONSE/PATH/UNCONFIRMED/NONE】 | 【填写】 | 【填写】 | 【填写】 | `【填写】` | 【填写：建议交给哪个专项 skill；无则写无】 |

若无敏感 sink，保留一行 `NONE`，说明已追踪到的终点。若只追到接口/Manager/DAO 方法且实现缺失，使用 `UNCONFIRMED`，不要写推断的 SQL/Hibernate/文件/XML 细节。

## 5. 参数流向与可控性

| 参数 | Source | Transform 摘要 | Guard/覆盖 | Sink ID | 可控性 | 限制说明 |
|------|--------|----------------|------------|---------|--------|----------|
| 【填写】 | 【填写】 | 【填写】 | 【填写】 | 【填写】 | 【填写：完全可控/条件可控/受限可控/不可控/未确认/无敏感 Sink】 | 【填写】 |

## 6. 分支条件与路径状态

### 6.1 分支树

```text
【填写：关键分支树；无分支写“本调用链未发现影响 sink 的条件分支”】
```

### 6.2 分支判定表

| 分支 | 条件 | 条件来源 | 结果 | 路径状态 | 对可控性的影响 |
|------|------|----------|------|----------|----------------|
| 【填写】 | 【填写】 | 【填写】 | 【填写】 | 【填写：可达/条件可达/受限可达/阻断/未确认】 | 【填写】 |

## 7. 下游交接结论

| 项目 | 内容 |
|------|------|
| 已确认事实 | 【填写：调用链和 sink 事实】 |
| 不能下的结论 | 本报告不确认漏洞成立，不给 CVSS，不给破坏性 PoC |
| 建议下游 skill | 【填写：java-sql-audit / java-xxe-audit / java-file-upload-audit / java-file-read-audit / java-deserialization-audit / 无】 |
| 仍需确认 | 【填写：运行配置、鉴权、组件版本、环境、反射目标等；无则写无】 |

---

## 输出自检

- [ ] 8 个主章节均存在且顺序正确。
- [ ] 不含未替换的填写占位、dollar-brace 变量占位或英文省略号占位。
- [ ] 请求模板不含 dollar-brace 变量格式；结构占位使用双花括号格式，样例请求使用安全实值。
- [ ] 入口证据可定位到真实配置或代码。
- [ ] 调用链没有跳层，接口/父类/Mapper/工具方法已处理或说明限制。
- [ ] sink 类型使用 `CONTROLLABILITY_ANALYSIS.md` 中的统一分类。
- [ ] 没有把缺源码的 Manager/DAO 调用推断成 SQL/FILE/XML 等具体 sink。
- [ ] 可控性结论考虑了覆盖、校验、白名单、默认值和分支。
- [ ] 鉴权信息仅透传上游，未自行判定。
- [ ] 未写漏洞已确认、CVSS、CVE 修复版本或破坏性 PoC。
