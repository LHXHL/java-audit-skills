# 路由调用链追踪报告模板（简化版）

适用场景：未命中敏感 sink、链路很短、重复模式明显，或多方法入口中非重点方法。简化版仍必须独立追踪当前方法，不能复用其他方法结论。

硬约束：

1. 输出必须包含 5 个主章节：`0` 到 `4`。
2. 所有 `【填写】` 必须替换为真实内容。
3. 不能把 sink 可达写成漏洞成立。
4. 文件建议放在 `route_tracer/{route_slug}/`。
5. 请求模板禁止使用 `${param}`；结构占位只允许 `{{param}}`，安全样例必须用实际低风险值。
6. Manager/DAO/Repository 方法名不是 sink 证据；实现缺失时写 `UNCONFIRMED`。
7. 报告只能包含模板定义的 5 个主章节，不得添加 `## 输出自检`、模型验收、技能源校验或测试过程说明。
8. 文件中禁止使用三个连续英文点号作为省略；代码片段必须摘录真实语句，不得用省略号占位。

---

# 【填写：项目名】 路由调用链追踪报告（简化版）

## 0. 上游上下文

| 项目 | 内容 |
|------|------|
| 路由/入口 | 【填写】 |
| route_id | 【填写：若无填无】 |
| 上游来源 | 【填写】 |
| 鉴权状态 | 【填写：原样透传上游鉴权信息；未提供则写未提供】 |
| 本报告边界 | 只追踪调用链、sink 和可控性，不判定漏洞成立 |

## 1. 入口与请求

| 项目 | 内容 |
|------|------|
| 入口类方法 | 【填写：Class.method(signature)】 |
| 入口证据 | 【填写】 |
| HTTP/SOAP 方法 | 【填写】 |
| 参数 | 【填写：参数路径、类型、来源】 |
| 分析限制 | 【填写：无或具体限制】 |

```http
【填写：安全样例请求或 SOAP 片段；禁止 ${param}；无 HTTP 请求时写 N/A 和原因】
```

## 2. 调用链与 Sink

```text
【填写：入口 -> Service/Util -> Sink/NONE；按真实层级填写】
```

| Sink 类型 | 位置 | 到达参数 | 关键证据 | 下游建议 |
|-----------|------|----------|----------|----------|
| 【填写：SQL/FILE_READ/FILE_WRITE/XML/DESERIALIZE/COMMAND/HTTP/LDAP/EXPRESSION/RESPONSE/PATH/UNCONFIRMED/NONE】 | 【填写】 | 【填写】 | `【填写】` | 【填写】 |

## 3. 可控性与分支

| 参数 | Guard/覆盖 | 路径状态 | 可控性 | 限制说明 |
|------|------------|----------|--------|----------|
| 【填写】 | 【填写】 | 【填写：可达/条件可达/受限可达/阻断/未确认】 | 【填写：完全可控/条件可控/受限可控/不可控/未确认/无敏感 Sink】 | 【填写】 |

## 4. 下游交接结论

| 项目 | 内容 |
|------|------|
| 已确认事实 | 【填写】 |
| 不能下的结论 | 本报告只提供调用链、sink 和可控性证据；漏洞确认、复现材料和修复方案交给下游专项 skill |
| 建议下游 skill | 【填写】 |
| 仍需确认 | 【填写：只写与当前调用链和 sink 直接相关的补证项；非调用链安全基线不要展开】 |
