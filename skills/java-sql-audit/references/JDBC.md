# 字符串查询机制兼容参考

本文件作为字符串查询机制参考；以项目证据中的真实执行点为准。

## 关注点

- 查询字符串是否由 `QUERY_TEMPLATE` 和 `QUERY_FRAGMENT` 组合。
- `REQUEST_PARAM` 是否进入查询结构位置。
- 结构位置是否经过闭合集合映射。
- 值位置是否有真实 `VALUE_BINDING`。
- 查询执行是否真实可达。

只有构造字符串但没有执行证据时，结论最高为待验证或不可确认。
